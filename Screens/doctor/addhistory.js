import React from 'react';
import {View, Text, StyleSheet, StatusBar, Button} from 'react-native';
import Colors from '../../colors/Colors';

const AddHistory = (props) => {
  return (
    <View style={{flex: 1}}>
      <Text>Add new History of </Text>
    </View>
  );
};
export default AddHistory;
